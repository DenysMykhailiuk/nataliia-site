---
category: Layout
---

# StatRow

Three figures between hairline rules, inside the bio column. Fixed at three columns,
dropping to two on phones — so three `Stat`s, not four.

```jsx
<StatRow />
```
