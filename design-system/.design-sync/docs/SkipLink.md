---
category: Primitives
---

# SkipLink

Keyboard-only jump to `#main`. Clipped out of view until it takes focus, then it appears as
a clay pill at the top left. First element in the body on every page.

```jsx
<SkipLink />
<SiteHeader current="/" />
```
